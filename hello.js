function sayhi(name) {
  console.log(`hello ${name}`);
}

module.exports = sayhi;

function add(a, b) {
  return a + b;
}
module.exports = add;

// in the above code i write 2 function and export 2 times
